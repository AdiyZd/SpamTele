let Data = 0
const Jkueri = 20
const Total = 100

for(Data; Data <= Total; Data++) {
    
    const out = Data + 1
    
    if (out <= Jkueri) {
        console.log("ini adalah looping sederhana denga js: " + out);
    } else {
        console.log("Ini hasil fale: " + out);
    }
}